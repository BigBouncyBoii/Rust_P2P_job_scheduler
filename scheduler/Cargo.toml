[package]
name = "scheduler"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = {version = "1", features = ["full"]}
serde = {version = "1", features = ["derive"]}
petgraph = "0.7"
rand = "0.8"
chrono = "0.4"